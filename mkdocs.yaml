site_name: Pokétop RPG
# theme: torillic
docs_dir: source
site_dir: docs
site_url: https://TEParsons.github.io/poketop

nav:
  - ...

hooks:
  - utils/ipa.py
  - utils/links.py

exclude_docs: |
  desktop.ini

validation:
  absolute_links: ignore

markdown_extensions:
  - nl2br
  - extra
  - wikilinks

plugins:
  - awesome-pages
